# Web React Example

This example shows the current `bubbleui-js` React primitives and design tokens.

Run it from the example folder:

```bash
cd examples/web-react
npm install
npm run dev
```

The example consumes the local package through:

```text
file:../../packages/bubbleui-js
```
