# Python EXE Example

This example shows all current `bubbleui` Python/Flet components in one page.

Run it from the repository root:

```bash
pip install -e .
python examples/python-exe/all_components.py
```

Use this folder when validating the Python desktop or packaged `exe` side of the UI library.
